kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: control-plane
  - role: worker
# this config is generating error with LoadBalancer
# even my computer using kind couldn't create